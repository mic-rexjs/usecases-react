export enum UseCaseModeErrors {
  UnsupportedGlobal = 'useUseCase does not support "global" mode with entity usecase',

  UnsupportedStateless = 'useUseCase does not support "stateless" mode with non-entity usecase',
}
